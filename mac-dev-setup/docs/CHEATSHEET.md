# 快捷键速查（macOS）

Mac 版和 Windows 版快捷键完全相同，只是某些地方 `Ctrl` 可能习惯换成 `Cmd`，但 WezTerm 统一用 `Ctrl` 保持跨平台一致。
如果你想把 Mac 上的部分快捷键改成 `Cmd`，在 `wezterm/config/keybings.lua` 里把 `mods = 'CTRL'` 改成 `mods = 'SUPER'` 即可。

详情见 Windows 版：[../../win-dev-setup/docs/CHEATSHEET.md](../../win-dev-setup/docs/CHEATSHEET.md)
